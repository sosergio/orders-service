{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/orders-service-web-api/OrdersService.WebApi.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}